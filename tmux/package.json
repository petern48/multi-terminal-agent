{
  "name": "multi-terminal-tmux",
  "version": "0.1.0",
  "description": "Provider-agnostic MCP server for multi-terminal control via tmux",
  "main": "./out/index.js",
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "start": "node out/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.3"
  }
}
